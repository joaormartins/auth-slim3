{% extends 'templates/app.twig' %}


{% block content %}
<div class="row justify-content-center">
	<div class="col-md-6">
		<form class="form" method="post" action="{{ path_for('auth.login') }}">

			<div class="form-group">
				<label>Username / Email Address:</label>
				<input type="text" name="username" class="form-control" placeholder="Username or email" value="{{ old.username }}" required>
			</div>

			<div class="form-group">
				<label>Password:</label>
				<input type="password" name="password" class="form-control" value="{{ old.password }}" placeholder="***" required>
			</div>

			<div class="remember">
				<label>
					<input type="checkbox" name="remember">
					Remember Me
				</label>
			</div>

			<div class="mt-4">
				<input type="submit" value="Login" class="btn btn-primary">
			</div>
		</form>
	</div>
</div>
{% endblock %}